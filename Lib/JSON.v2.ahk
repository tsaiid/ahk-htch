#Requires AutoHotkey v2.0

class JSON {
    static Load(text, reviver := "") {
        throw Error("JSON.Load is not implemented in this v2 compatibility wrapper.")
    }

    static Dump(value, replacer := "", space := "") {
        throw Error("JSON.Dump is not implemented in this v2 compatibility wrapper.")
    }
}
